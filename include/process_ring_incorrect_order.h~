#ifndef PROCESS_RING_HI
#define PROCESS_RING_HI
#include<xinu.h>
#include<stdlib.h>

#define NUM_PROCESSES 4

extern volatile int box1[NUM_PROCESSES];
extern volatile int rounds1;
void counterDecrementerOutOfOrder(int);
#endif
